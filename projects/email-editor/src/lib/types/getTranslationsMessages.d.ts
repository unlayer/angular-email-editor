import { Translations } from './StateTypes';
import { StockLocale } from './TranslationTypes';
export declare function getTranslationsMessages({ locale, translations, }: {
    locale: StockLocale;
    translations: Translations;
}): any;
